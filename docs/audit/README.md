# 감사 대장

감사자는 `.claude/agents/` 에 있다. 여기에는 **감사를 돌린 뒤 남는 것**이 쌓인다 —
부적합과 그 판정, 그리고 감사자 자신을 좁히는 대기 항목.

## 닫는 규칙

**「고쳤다」가 닫지 않는다. 재감사가 닫는다.**

고친 사람이 스스로 닫으면 「고쳤다고 생각한 것」과 「고쳐진 것」이 갈리고,
갈린 줄 아무도 모른다. 그래서 고친 뒤에도 상태는 **재감사 대기**이며, 같은
목적으로 다시 감사해 재발하지 않은 것을 확인할 때 닫힌다.

**감사 기록은 지시가 아니다.** 판정이 틀렸을 수 있고, 가르는 것은 저자다.
반박하려면 근거를 적는다 — 반박도 대장에 남아 다음 감사가 그것을 본다.

판정은 셋 중 하나다.

| 판정 | 무엇 |
|---|---|
| **고침** | 이번 조각 안이라 지금 고쳤다. 재감사가 닫는다 |
| **등록** | 이번 조각 밖이다. 언제 닫을지 적고 그때까지 열어 둔다 |
| **반박** | 부적합이 아니다. 근거를 적는다 |

---

---

## 부적합 대장

회차를 넘어 한 표로 본다 — **닫는 권한은 낸 회차가 아니라 다음 회차에 있다.**

| NC | 무엇 | 낸 회차 | 상태 | 닫은 근거 |
|---|---|---|---|---|
| 1 | 자재군 배정이 마이그레이션과 시드 두 벌인데 견주는 테스트가 없다 | ① | **닫힘** | ② 재감사 적합 — 돌연변이 M1 · M7 이 **양쪽에서** 물었다 |
| 2 | 마이그레이션의 데이터 단계가 CI 에서 한 번도 돌지 않는다 | ① | **닫힘** | ② 재감사 적합 — 돌연변이 일곱이 전부 데이터 단계를 지나며 물었다 |
| 3 | upgrade 가 수입 기준을 지우고 기본값으로 다시 심는다 | ① | **닫힘** | ② 재감사 적합 — M3 · M4 |
| 4 | 목록 밖 원자재가 있으면 원인을 말하지 않고 멈춘다 | ① | **부분 닫힘** | 원자재 경로는 M6 로 물었다. 검사항목 경로와 그룹 자리가 남아 **NC-10 으로 잇는다** |
| 5 | `docs/schema.md` 가 갱신되지 않아 표의 모양이 두 곳에서 다르다 | ① | **닫힘** | ② 재감사 적합 |
| 6 | `docs/schema-2단계.md` §15 가 자기 문서 안에서 모순된다 | ① | **닫힘** | ② 재감사 적합 |
| 7 | 마이그레이션과 모델의 **칸 순서**가 다르다 | ① | **닫힘 (반박)** | ② 도 반박이 타당하다고 판정했다 |
| 8 | downgrade 가 운영자가 더한 자재군 코드까지 지운다 | ① | **닫힘** | ② 재감사 적합 — 탐침에서 운영자의 `금속` 코드가 되돌린 뒤에도 남았다 |
| 9 | downgrade 가 「베낀 줄만 지운다」고 적고 남의 줄과 σ 를 조용히 지운다 | ② | **닫힘** | ③ 재감사 적합 — 탐침 P1·P2 가 둘 다 잡았고 M-A·M-B 가 각각 물었다. 거짓 실패 없음(P3·P4·P7), 멈춘 downgrade 가 완전히 롤백됨(P10) |
| 10 | 멈출 때 이유를 말하는 것이 한 경로만 검사되고, 한 경로는 여전히 말하지 않는다 | ② | **닫힘** | ③ 재감사 적합 — M-G·M-M 이 RAISE 둘을, M-H·M-I 가 그룹 둘을 각각 물었다. **다만 이 고침이 NC-13 을 열었다** |
| 11 | 「줄바꿈조차 잡는다」가 사실이 아니다 | ② | **부분 닫힘** | 독스트링 둘 · `CLAUDE.md` 는 적합(P8·M-E). 같은 파일 셋째 자리가 남아 **NC-14 로 잇는다** |
| 12 | 「옛 DB」 픽스처가 옛 DB 와 다르다 | ② | **닫힘** | ③ 재감사 적합 — 옛 시드와 열두 칸 일치. M-C·M-D·M-F 가 물었다 |
| 13 | 되돌리기가 **운영자가 먼저 만든** 자재군 코드·그룹을 조용히 지운다 | ③ | **부분 닫힘** | ④ 재감사 — 탐침 P-13·P-18 이 둘 다 살아남는 것을 보였다. 「한 곳에 모았다」가 지우는 쪽까지 가지 않았고(**NC-18**) 「지우는 자리」를 세는 방식이 한 자리를 빠뜨렸다(**NC-17**) |
| 14 | 「줄바꿈만으로도 갈린다」가 `test_migrations.py` 셋째 자리에 남아 있다 | ③ | **부분 닫힘** | ④ 재감사 — 일곱 자리 중 여섯은 사실. `pyproject.toml` 이 남아 **NC-19 로 잇는다** |
| 15 | 자재군 코드의 **이름 · 정렬 · 설명**이 두 벌인데 견주는 것이 없다 | ③ | **닫힘** | ④ 재감사 적합 — M-J · M-K · M-L 을 **시드와 마이그레이션 양쪽에서** 다시 돌려 전부 물었다 |
| 16 | 「임의」 σ 가 사라질 때도 「**실측** σ」라고 말한다 | ③ | **부분 닫힘** | ④ 재감사 — 말은 맞았으나 지키는 것이 없고(M-P 가 안 물었다) 같은 파일 머리말이 아직 「실측」이다. **NC-20 으로 잇는다** |
| 17 | 되돌리기가 **사람이 고친 품목의 자재군 배정**을 조용히 지운다 | ④ | **닫힘** | ⑤ 재감사 적합 — `RM-12` 를 직접 재현해 멈추고 완전히 롤백되는 것을 보였다. M-A · M-B 가 물었고 정상 경로는 막히지 않는다 |
| 18 | 「심은 것만 지운다」의 목록이 아직 두 벌이다 — 지우는 쪽이 이름을 직접 적는다 | ④ | **닫힘** | ⑤ 재감사 적합 — 목록 하나를 셋이 부른다. M-C 가 물었고, 시드와의 두 벌은 M-F 가 견주는 것을 확인했다 |
| 19 | 「줄바꿈하면 갈린다」가 `pyproject.toml` 에 네 번째로 남아 있다 | ④ | **닫힘** | ⑤ 재감사 적합 — `git grep` 전수(다른 표현 포함). 다섯째 자리 없음 |
| 20 | NC-16 의 넓힘을 지키는 것이 없고, 같은 파일이 아직 「실측」이라 말한다 | ④ | **닫힘** | ⑤ 재감사 적합 — M-D 가 물었다. 「실측」 잔여 전수, `relfilenode` 주장도 직접 재어 사실 확인 |
| 21 | 이 리비전이 표를 다시 쓰고 쓰기를 멈추는데 그 사실이 적혀 있지 않다 (W-1) | ④ | **부분 닫힘** | ⑤ 재감사 — 주장 여덟 중 일곱이 구문 · `pg_locks` · `relfilenode` 와 맞다. CHECK 수가 어긋나 **NC-27 로 잇는다**. **심각도는 확인불가로 남는다** |
| 22 | 초기 리비전의 downgrade 가 표 열넷을 되돌릴 수 없이 지우는데 표시가 없다 | ④ | **닫힘** | ⑤ 재감사 적합 — 14 표 일치, `alembic history -v` 로 도달. 말이 가드인 유일한 행이라는 것은 OB-10 |
| 23 | 되돌리기가 베낀 줄의 **규격**을 조용히 지운다 — 가드가 σ 만 본다. **같은 모양의 다섯 번째** | ⑤ | 열림 | 고쳤다 — ⑥ 이 닫는다. 심각도는 **확인불가**(배포처·운영 데이터가 없으면 낮음, 있으면 높음) |
| 24 | 리비전 **머리말**이 아직 「베낀 줄만 지운다」고 말한다 — 같은 파일 본문이 거짓이라 적어 둔 명제다 | ⑤ | 열림 | 고쳤다 — ⑥ 이 닫는다 |
| 25 | 되돌리기 가드 다섯 중 `code_groups` 하나만 검사가 없다 | ⑤ | 열림 | 고쳤다 — ⑥ 이 닫는다 |
| 26 | `audit-data` 항목 3 자체 — 구문 열이 Alembic 저장소에서 0 건 · W-4 기한 초과 · **적합으로 가는 길이 없다** | ⑤ | 열림 | 고쳤다 — ⑥ 이 닫는다 |
| 27 | NC-21 독스트링의 「CHECK 여섯」이 실제로는 넷이다 | ⑤ | 열림 | 고쳤다 — ⑥ 이 닫는다 |

| 28 | 「사람이 넣은 값이 따라간다」의 upgrade 설명이 아직 「**실측** σ」로 좁다 | ⑤-b | 열림 | 고쳤다 — ⑥ 이 닫는다. NC-16 이 가드에서 고친 좁힘의 잔여다 |
| 29 | 「**네 표**의 쓰기가 멈춘다」가 실제로는 셋이다 — `code_groups` 는 `INSERT` 뿐이라 막지 않는다 | ⑤-b | 열림 | 고쳤다 — ⑥ 이 닫는다 |
| 30 | `audit-data` 넷째 눈이 「**적힌 것이 맞는가**」를 묻지 않아, 자기가 낸 부적합의 고침을 검사하지 못한다 | ⑤-b | 열림 | 고쳤다 — ⑥ 이 닫는다 |

> 23 ~ 30 은 고쳤지만 **열려 있다.** 「고쳤다」가 닫지 않는다 — 감사 ⑥ 이 닫는다.

### 회차 ⑤ 는 둘이다 — 번호 대조

같은 커밋 `30de559` 를 **두 세션이 따로** 감사했고 둘 다 자기 번호를 23 부터 붙였다.
대장은 **먼저 도착한 쪽(⑤-a)의 번호를 쓰고**, ⑤-b 의 번호는 아래로 옮긴다.

| ⑤-b 가 붙인 번호 | 대장 번호 | 관계 |
|---|---|---|
| NC-23 (독스트링 모순) | **NC-24** | 같은 자리. ⑤-a 가 먼저 냈고 `ecc6455` 가 고쳤다. **⑤-b 가 잔여를 하나 더 봤다 → NC-28** |
| NC-24 (지워질 줄의 규격) | **NC-23** | 같은 자리. 번호만 뒤집혔다 |
| NC-25 (적힌 수 둘) | **NC-27 + NC-29** | ⑤-a 는 CHECK 수만 봤고 ⑤-b 가 표 수도 쟀다 |
| NC-26 (넷째 눈) | **NC-30** | ⑤-a 의 NC-26(구문 열 · 적합 경로)과 **다른 결함**이다. 같은 항목의 셋째 구멍 |

**독립한 둘이 같은 자리를 짚었다는 것이 값이다.** ⑤-a 와 ⑤-b 가 서로 모르는 채
「되돌리기가 지워질 줄의 값을 가져간다」와 「독스트링이 자기 코드와 모순된다」를
**둘 다** 냈다. 그리고 각자 상대가 못 본 것을 하나씩 더 봤다 — ⑤-a 는 구문 열이
Alembic 저장소에서 0 건인 것을, ⑤-b 는 `pg_locks` 를 실제로 폴링해 표 수가 틀린 것을.

---

## 감사 ① — 데이터 · 쓰기 경계 (2026-09-16)

- **감사한 커밋**: `57ee94d` (자재군 축)
- **목적**: 데이터 · 쓰기 경계 하나
- **기록**: [PR #2 코멘트](https://github.com/pdw96/ERP/pull/2#issuecomment-5693342820) ·
  [판정](https://github.com/pdw96/ERP/pull/2#issuecomment-5693464544)
- **반영**: `ced42e5`
- **결과**: 부적합 여덟 — 고침 일곱 · 반박 하나

### 고친 자리 — 무엇을 어떻게

**NC-2 · NC-1 — 검사가 없던 자리에 검사를 세웠다.**
대조 테스트는 **빈 스키마**에 올리므로 데이터 단계(`WHERE EXISTS (SELECT 1 FROM
items)`)가 통째로 건너뛰어지고 있었다. 저장소에서 처음으로 **데이터를 옮기는**
마이그레이션인데 가장 검사가 얕은 자리에 있었다는 판정은 옳다.
`tests/test_migrations.py` 에 넷을 더했다 — 그중
`test_both_roads_reach_the_same_material_groups` 가 두 길(빈 DB→시드 ·
옛 DB→마이그레이션)을 실제로 돌려 자재군 배정을 견주므로 NC-1 도 함께 덮는다.
**무는지 확인했다** — 마이그레이션의 `RM-12` 만 다른 무리로 바꾸니 그 테스트가
정확히 실패했다.

**NC-3 — 지우지 않고 옮기게 바꿨다.**
이 마이그레이션이 존재하는 근거가 「사람이 고친 값을 재시드가 덮어쓰는 쪽이 더
큰 사고다」인데 `DELETE` 후 하드코딩 재삽입이 바로 그 재시드와 같은 일을 하고
있었다. 옛 여덟 줄을 `UPDATE` 로 첫 무리에 옮기고, 두 무리 이상에 걸치는 항목만
그 줄을 베껴 세운다. 되돌릴 때도 베낀 줄만 지운다.

> **감사 제안에서 한 걸음 더 갔다 — σ 는 베끼지 않는다.** 규격 상·하한은 고객이
> 정하는 것이라 같은 항목이면 무리가 달라도 쓸 수 있지만, σ 는 **잰 값**이고 새
> 무리에서는 잰 적이 없다. 베껴 오면 재지 않은 것을 쟀다고 적는 것이 되므로
> 「미정」으로 세운다.

**NC-4 — 무엇이 남았는지 말하고 멈춘다.**
`RAISE EXCEPTION` 이 채우지 못한 품목 코드와 검사항목을 이름으로 말한다. 예전에는
바로 뒤의 CHECK 가 대신 터져 「제약 위반」만 보였다.

**NC-5 · NC-6 — 문서를 표의 실제 모양에 맞췄다.**
`schema.md` §1 에 자재군 칸과 양방향 CHECK 를, §12 에 대리키·유일키·양방향 CHECK
를 적고 「값이 늘 수 있는 그룹」을 아홉으로 고쳤다. `schema-2단계.md` §15 의 옛
문장을 결론에 맞췄다.

**NC-8 — 심은 셋만 지운다.**
자재군은 「값이 늘 수 있는 그룹」이라 넷째 무리가 늘었을 수 있다. downgrade 가
그룹째 지우면 사람이 넣은 사실이 함께 사라지므로, 이 리비전이 심은 셋만 지우고
그룹은 **남은 값이 없을 때만** 지운다.

### NC-7 — 부적합이 아니다

**판정**: 칸 순서가 다른 것은 맞다. 그것이 **요구를 어긴 것은 아니다.**

`docs/PRD-1단계.md` 의 성공 기준 ①이 비교 대상을 열거한다 — 「자료형 · 널 허용 ·
기본키 · 외래키 · 유일키 · CHECK 까지 비교하는 테스트가 있다」. **칸 순서는 그
목록에 없다.** 요구된 적 없는 것을 지키지 않은 것은 부적합이 아니다.

그리고 빠진 것이 우연이 아니다. 마이그레이션의 `add_column` 은 끝에 붙이고
모델은 선언한 자리에 두므로 두 길의 순서는 **구조적으로** 다르다. 맞추려면 칸을
더할 때마다 표를 다시 만드는 리비전이 필요하고, 얻는 것이 없다 — 시드와
마이그레이션의 `INSERT` 는 전부 칸 이름을 적고 `SELECT *` 의 순서에 기대는 코드가
없다. 감사 자신도 「오늘 실제로 새는 곳은 없다」고 적었고 고치지 말 것을 제안했다.

**다만 제안은 받았다.** 「이 테스트를 『완전히 같다』로 읽을 위험」은 타당하므로
`_columns()` 독스트링에 무엇을 견주지 않는지와 왜 허용하는지를 적었다. 그것은
부적합을 닫은 것이 아니라 제안을 수용한 것이다.

---

## 감사 ② — 데이터 · 쓰기 경계 (2026-09-16, 재감사)

- **감사한 커밋**: `ced42e5` (감사 ① 판정 반영분)
- **목적**: 데이터 · 쓰기 경계 — ① 과 같다. 회차 비교가 되어야 하므로 항목을 바꾸지 않았다
- **기록**: [PR #2 코멘트](https://github.com/pdw96/ERP/pull/2#issuecomment-5694335608)
- **반영**: 이 커밋
- **결과**: ① 의 여섯을 닫고, 하나를 부분 닫힘으로 잇고, 넷을 새로 냈다

**① 과 달라진 것 — 이번에는 돌렸다.** ① 은 읽기만 했고 스스로 「실행 검증이 전부
빠졌다」고 적었다. ② 는 PostgreSQL 16 을 세우고 검사 넷을 돌린 뒤 **돌연변이
아홉**으로 어느 테스트가 무는지 확인하고 **탐침 다섯**으로 테스트가 덮지 않는
경로를 직접 돌았다. 저장소는 한 글자도 고치지 않았다.

그 방식이 ① 이 못 본 것을 넷 찾았다. 특히 **M7** — ① 의 판정에서 나는
마이그레이션쪽만 망가뜨려 테스트가 무는 것을 확인했는데, ② 는 **시드쪽**도
망가뜨렸다. 목록이 두 벌이라는 것은 어느 쪽을 고쳐도 갈린다는 뜻이고, 한쪽만
확인한 것은 절반만 확인한 것이었다.

### 고친 자리 — 무엇을 어떻게

**NC-9 — 되돌리기가 무엇을 지우는지 말하고 멈춘다.**
주석은 「베낀 줄만 지운다」고 말했는데 조건은 `id` 가 최솟값이 아닌 **전부**라서
이 리비전이 베끼지 않은 줄도 함께 지웠다. 탐침이 운영자의 `금속` 줄과 베낀 줄의
실측 σ 가 사라지는 것을 재현했다.

**줄이는 것 자체는 피할 수 없다** — 옛 기본키가 항목마다 한 줄만 받는다. 피할 수
있는 것은 **조용한 것**이라, upgrade 의 `_STOP_IF_ANYTHING_WAS_LEFT_BEHIND` 와 같은
모양으로 둘을 보고 이름을 말하고 멈춘다: 이 리비전이 세우지 않은 줄, 그리고 남길
한 줄이 아닌 자리의 실측 σ. 주석도 코드가 실제로 하는 일에 맞췄다.

> **이 지적의 핵심은 NC-8 과 같은 모양이라는 것이었다.** 부적합 하나를 고치고
> 같은 패턴을 한 표 옆에 남긴 것이다. 그래서 이 리비전이 지우는 자리를 전부
> 다시 봤다 — `common_codes`(NC-8 에서 고침) · `code_groups`(같은 커밋) ·
> `process_inspection_standards`(NC-9). 지우는 자리는 그 셋뿐이다.

**NC-10 — 두 자리를 메웠다.**
수입 검사항목 RAISE 경로에 테스트를 붙였다(도달 못 하는 방어 코드가 아니라
**검사되지 않는** 방어 코드였다). 그리고 `code_groups` INSERT 는 이미 있으면
넘어가게 하고, 있되 `value_fixed` 가 참이면 — 자재군과 정반대의 뜻이라 —
이름을 말하고 멈춘다.

**NC-11 — 고친 것은 테스트가 아니라 문장이다.**
`pg_get_constraintdef` 가 식을 정규화하므로 공백과 줄바꿈은 비교 전에 사라진다.
**직접 재현했다** — 줄바꿈을 넣은 CHECK 와 넣지 않은 CHECK 가 같은 문자열로
돌아오고, `IN ('수입')` 이 `= '수입'::text` 로 다시 쓰이는 것까지 정규화된다.
독스트링 둘과 `CLAUDE.md` 를 「뜻이 달라지면 잡는다」로 고쳤고, 줄바꿈 금지는
남기되 근거를 「이 테스트가 잡는다」에서 **「diff 가 커진다」**로 바꿨다.
「적어 두기만 하고 강제하지 않는 규칙을 만들지 않는다」에 걸리던 자리다.

**NC-12 — 「옛 DB」를 진짜 옛 DB 로 맞추고, 그것을 지키게 했다.**
픽스처가 `time_variant` 를 적지 않아 여덟 줄이 전부 `FALSE` 로 섰는데, 옛 시드는
수분 · 점도 · 색차 · 포장에 `TRUE` 다. 만료 재검사가 보는 유일한 잣대라 2단계에
그것이 들어오면 **한 번도 존재한 적 없는 상태 위에서 초록이 뜬다.**
`time_variant` · `warning_ratio` · `sigma_source` 를 옛 시드와 같게 적었다.

> **고치고 나서 돌연변이로 물어 보니 물지 않았다.** 픽스처를 다시 틀리게 해도
> 아무 테스트도 실패하지 않았다 — 감사가 「오늘 새는 곳은 없다」고 적은 그대로인데,
> 그 말은 **이 수정을 지키는 것이 없다**는 뜻이기도 하다. 「적어 두기만 하고
> 강제하지 않는 규칙을 만들지 않는다」에 걸리는 자리다.
>
> 그래서 두 길 비교를 **값까지** 넓혔다. 마이그레이션이 옛 줄을 지우지 않고
> 옮기므로 두 길의 값도 같아야 하고, 그 덕에 픽스처가 옛 DB 와 같은지가 함께
> 지켜진다. 다시 물어 보니 이번에는 물었다.

**OB-3** — `master.py` 의 주석 블록 사이에 빈 줄 하나. 「단위 변경은 수정이 아니라
새 품목」이라는 미룬 결정이 `material_group` 의 설명처럼 읽히던 자리다.

### 반박하지 않았다

넷 다 코드에서 확인했고 전부 사실이었다. NC-9 의 「주석과 코드가 다르다」는 읽어서,
NC-11 은 PostgreSQL 에서 **직접 재현해서**, NC-12 는 옛 시드(`7aa9fcd`)와 대조해서
확인했다. ② 가 NC-7 반박을 타당하다고 판정한 것도 받는다.

---

---

## 감사 ③ — NC-9 ~ 12 재감사 (2026-09-16, 좁힘)

- **감사한 커밋**: `e1666b0`
- **목적**: NC-9 ~ 12 가 닫혔는지 하나. ② 가 실행까지 포함해 판정한 여덟 항목은 다시 돌리지 않았다
- **기록**: [PR #2 코멘트](https://github.com/pdw96/ERP/pull/2#issuecomment-5697218149)
- **반영**: 이 커밋
- **결과**: 셋을 닫고, 하나를 부분 닫힘으로 잇고, **넷을 새로 냈다**

탐침 열하나와 돌연변이 열셋. ② 보다 더 밀었고, 그만큼 더 나왔다.

### NC-13 — 내 고침이 연 자리다

가장 무거운 발견이다. **NC-10 을 고치면서 새로 열었다.**

「그룹·코드가 이미 있으면 넘어간다」를 넣은 순간 운영자가 먼저 만든 줄이 살아서
upgrade 를 건너오게 됐는데, downgrade 는 **이름으로** 셋을 지운다. `806b179` 이전에는
이 상태가 아예 설 수 없었다 — 그때는 겹치면 유일키 위반으로 멈췄다. 고침이 없던
상태를 만들고, 그 상태에서 옛 코드가 남의 것을 지운 것이다.

**NC-8 · NC-9 가 지적한 바로 그 모양**이 세 번째로 나왔다. 그래서 이번에는 고치는
김에 **심는 값을 한 곳에 모았다**(`_MATERIAL_GROUP_CODES`) — 넣는 쪽과 되돌릴 때
지키는 쪽이 같은 것을 부르므로, 한쪽만 고쳐서 「심은 것만 지운다」가 거짓이 되는
길을 닫았다.

### NC-15 — 감사가 「등록이 맞을 수도 있다」고 남긴 판단

**고치는 쪽으로 갈랐다.** 이 조각이 낸 코드 셋이고(`57ee94d`), 고치는 자리가
`_incoming_standards()` 바로 옆이라 값이 싸다. 등록해 두면 다음 조각에서 이미 갈린
뒤에 발견한다.

감사가 「M-J · M-K · M-L 셋을 한꺼번에 어긋내도 167 passed」라고 적었다 —
**셋 다 다시 돌려 이제 무는 것을 확인했다.**

> **한 번 잘못 볼 뻔했다.** M-L(그룹 이름)을 처음 돌렸을 때 「안 물었다」가 나왔는데,
> 치환 패턴이 공백 하나 때문에 안 먹은 것이었다. 돌연변이가 실제로 들어갔는지부터
> 확인하지 않으면 **테스트가 못 잡았다고 잘못 적게 된다.**

### NC-14 · NC-16 — 말과 코드를 맞춘다

NC-14 는 `test_migrations.py` 의 셋째 자리다. 같은 파일 두 곳을 고치면서 **그 문장이
설명하는 바로 그 테스트**의 독스트링을 지나쳤다. NC-16 은 조건이 `sigma IS NOT NULL`
인데 말이 「실측」이었다 — **조건을 좁히지 않고 말을 넓혔다.** 좁히면 운영자가 넣은
「임의」 σ 가 다시 조용히 사라진다.

### 반박하지 않았다

넷 다 코드에서 확인했고 전부 사실이었다. NC-13 은 탐침을 다시 밟아서, NC-14 는
`grep` 전수로, NC-15 는 돌연변이 셋을 다시 돌려서, NC-16 은 조건과 `SIGMA_SOURCES`
를 견줘서 확인했다.

OB-4(가드 둘이 겹치면 첫 메시지만) · OB-5(시드 순서가 남길 줄을 정한다)는 감사가
고칠 것 없다고 판정했고 동의한다.

### W-1 이 세 번째로 재이지 못했다

감사 ③ 도 「이번에도 재지 못했다 — 항목이 들어가야 잰다」고 **확인불가**에 적었다.
대장이 기한을 「PR #2 재감사 전」으로 적은 지 세 회차가 지났다. 다음 감사 전에
`audit-data` 에 넣지 않으면 네 번째로 같은 자리를 지나친다.

---

## 감사 ④ — NC-13 ~ 16 재감사 + W-1 첫 측정 (2026-09-16, 좁힘)

- **감사한 커밋**: `a8781f7`. 문서 커밋 `cd1a4b1`(W-1 반영)은 코드 범위 밖이다
- **목적**: NC-13 ~ 16 이 닫혔는지 하나. ② 가 실행까지 포함해 판정한 여덟은 다시 돌리지 않았다
- **반영**: 이 커밋
- **결과**: 하나를 닫고, 셋을 부분 닫힘으로 잇고, **여섯을 새로 냈다**

돌연변이 열하나와 탐침 열. **치환은 전부 `git diff` 로 바뀐 줄을 눈으로 확인한 뒤**
돌렸다 — 감사 ③ 에서 공백 하나 때문에 「테스트가 안 물었다」로 잘못 읽을 뻔한 자리다.
그 규율이 이번에도 값을 냈다: **M-N 이 0 번 치환되어 「아직 재지 않은 것」으로
남았고**, 패턴을 고쳐 다시 재니 열여덟이 실패했다. 확인하지 않았다면 「안 물었다」로
적고 없는 구멍을 팔 뻔했다.

### NC-17 — 같은 모양의 **네 번째**다

이번 회차에서 가장 무겁다. 되돌리기가 **사람이 고친 품목의 자재군 배정**을 아무 말
없이 가져가고, 다시 올리면 `_ITEM_GROUPS` 의 값이 그 자리를 덮는다. 직접 재현했다 —

```
고친 뒤:            RM-12 = 액상수지     ← 사람이 고친 값
downgrade:          통과 (아무 말도 하지 않았다)
다시 올린 뒤:        RM-12 = 분체         ← 지어낸 값이 사람의 판단을 덮었다
```

하필 `RM-12` 는 이 대장이 **「코드로 판정할 수 없다. 사람이 본다」**로 적어 둔 바로
그 품목이다. 사람이 보고 고칠 것을 전제한 칸인데 그 고침이 남지 않았다.

**왜 세 회차가 놓쳤는가 — 세는 방식이 틀렸다.** ③ 은 「지우는 자리를 전부 다시 봤다
— 셋뿐이다」로 닫았는데, 그 셋은 `DELETE` 만 센 것이다. **칸을 떨어뜨리는 것도 지우는
것이다.** 세어야 하는 것은 구문이 아니라 **되돌릴 때 사라지는 사실**이었다.

### NC-18 — 「한 곳에 모았다」가 지우는 쪽까지 가지 않았다

`_MATERIAL_GROUP_CODES` 는 **넣는 쪽과 가드**만 불렀고 `DELETE` 는 여전히 이름 셋을
직접 적었다. 목록은 아직 두 벌이었다 — 「목록을 두 벌 두지 않는다」에 그대로 걸린다.

**M-O 가 그 값을 보였다.** 지우는 쪽에 `'금속'` 을 한 글자 더하는 것은 **NC-8 을 그대로
되돌리는 변경**인데 168 passed 였다. 기존 테스트는 기준 줄 가드가 **먼저** 터져 코드
`DELETE` 까지 닿지 않고, NC-8 은 감사 ② 의 **탐침으로만** 확인됐지 검사로 굳지 않았다.
닫힌 부적합이 검사 없이 닫혀 있으면 **조용히 다시 열린다.**

### NC-19 · NC-20 — 말과 코드가 또 갈렸다

둘 다 **같은 파일 안에서 같은 문장의 다음 자리를 지나친** 것이다. NC-19 는 `.toml` 이라
③ 의 `grep` 전수에서 빠졌고 — 하필 **E501 예외가 서 있는 근거**가 거짓 명제였다 —
NC-20 은 조건 바로 위에는 주석을 달고 열일곱 줄 위의 블록 머리말을 지나쳤다.

NC-20 에는 하나가 더 있다. **넓힘을 지키는 것이 없었다.** M-P 로 조건을 `'실측'` 으로
되좁혀도 168 passed 였다 — 「임의」 σ 를 밟는 줄이 한 줄도 없었다. 감사 ② 가 NC-12 에서
「고치고 나서 물어 보니 물지 않았다」로 잡은 것과 **똑같은 모양**이다.

### 새로 세운 검사 다섯

고침만으로는 닫히지 않는다. 다섯을 세우고 **전부 물리는 것을 확인**했다.

| 돌연변이 | 무엇을 어긋냈나 | 물었는가 |
|---|---|---|
| **M-N** | `_MATERIAL_GROUP_CODES` 에서 시트필름 제거 | 물었다 — 18 failed |
| **M-O** | 지우는 쪽에 남의 코드 `'금속'` 추가 (= NC-8 되돌리기) | 물었다 — `test_downgrade_keeps_a_code_someone_else_made` |
| **M-P** | σ 조건을 `'실측'` 으로 되좁힘 | 물었다 — `..._an_arbitrary_sigma_would_vanish` |
| **M-Q** | 코드 가드에서 `sort_order` 비교 제거 | 물었다 — `test_the_guard_compares_more_than_the_name` |
| **M-R** | NC-17 검사 무력화 | 물었다 — `..._someone_changed_an_item_group` |

거짓 실패가 없다는 것도 확인했다 — 정상 설치 경로(빈 DB → 마이그레이션 → 시드 →
downgrade → 다시 upgrade → downgrade)가 한 번도 멈추지 않고, 자재군 코드와 그룹이
깨끗이 사라진다.

### 반박하지 않았다

여섯 다 코드에서 확인했다. NC-17 은 **탐침을 직접 다시 밟아서**, NC-18 · NC-20 은
**돌연변이를 실제로 넣고 168 passed 를 보고서**, NC-19 는 `grep` 전수와 PostgreSQL
재현으로, NC-21 · NC-22 는 구문과 독스트링을 견줘서 확인했다.

**NC-22 는 이 조각이 낸 것이 아니다.** 1단계의 리비전이고 등록으로 두어도 됐다. 그런데
그 자리가 **NC-21 과 같은 모양이고 한 표 옆**이다 — 부적합 하나를 고치고 같은 패턴을
옆에 남기는 것이 NC-9 이 지적한 바로 그 경로라, 수평전개로 함께 고쳤다.

관찰 OB-6 은 「가드의 넓이를 테스트가 아니라 사람이 지키고 있다」였다. 부적합은
아니지만 M-Q 가 그 자리를 보였고 고치는 값이 싸서 검사를 세웠다. OB-7(값이 같으면
구분할 수 없다) · OB-9(잠금은 마이그레이션 전체 길이다)는 고칠 것이 없다는 판정에
동의한다. **OB-8 은 W-5 로 등록한다** — 올릴 때 조용히 넘어간 그룹 수정이 내릴 때
영구히 막는데, 그것이 드러나는 것은 화면이 서는 날이다.

### W-1 이 무엇을 잡았는가 — 항목의 첫 측정

**잡았다.** 항목이 들어가기 전 세 회차는 마이그레이션을 「논리적으로 통과하는가」로만
읽었고 세 번 다 적합으로 지나갔다. 다섯 눈으로 읽자 `65d31f8b7918` 이 **앞 셋에 전부
걸린다**는 것이 구문만 보고 나왔다 — `SERIAL` 기본키(표 다시 쓰기) · 외래키 둘과
CHECK 여섯(전수 검증) · 유일키(잠금 인덱스).

**결정적인 것은 「얼마나」를 묻지 않은 설계다.** 「얼마나」였다면 이번에도 확인불가로
끝났을 것이다. 그리고 **넷째 눈(「리비전에 적혀 있는가」)이 판정을 만들었다** — 앞
셋은 사실을 세울 뿐이고, 적혀 있지 않다는 것이 NC-21 이 됐다. 심각도를 몰라도 **지금
닫을 수 있는** 부적합이다. 「못 재니까 적합」을 피하면서 대장에 닫히지 않는 줄도
남기지 않는 자리를 항목이 정확히 만들어 냈다.

**덤으로 NC-22 가 나왔다.** 「되돌릴 때도 같은 것을 본다」는 마지막 줄이 초기 리비전의
`drop_table` 열넷을 보게 했다. 항목 3 을 읽다가 **항목 2** 의 부적합이 걸린 것이다.

**잡지 못한 것 셋.**

1. **심각도는 여전히 못 잰다.** 행 수 · 동시 쓰기 · 배포처가 저장소에 없다. 네 회차째
   확인불가이며, **닫히는 자리는 `audit-ops`(배포처가 생기는 단계)다**
2. **트랜잭션 경계와의 상호작용을 항목이 묻지 않는다.** 다섯 눈은 **구문 하나**를 본다.
   「전체가 한 트랜잭션이라 잠금이 마이그레이션 전체 길이만큼 유지된다」는 항목 밖에서
   나왔고, 다음 사람이 이 항목만 따라 읽으면 놓친다 → **W-4**
3. **W-2 는 이번에도 잴 대상이 없다.** 쓰기 경로가 아직 없다. 기한(「관문 1 조각」)이 맞다

---

## 감사 ⑤ — NC-17 ~ 22 재감사 + 「지운다」 축 전수 (2026-09-16, 좁힘)

- **감사한 커밋**: `30de559`. **반영**: 이 커밋
- **목적**: NC-17 ~ 22 가 닫혔는지 하나. ② 가 실행까지 포함해 판정한 여덟은 다시 돌리지 않았다
- **결과**: 다섯을 닫고, 하나를 부분 닫힘으로 잇고, **다섯을 새로 냈다**

**「지운다」 축을 `grep` 하지 않았다.** 네 회차가 놓친 이유가 파이썬 API 를 구문으로
센 것이므로, Alembic offline 모드(`upgrade|downgrade --sql`)로 **실제로 나가는 SQL
전부**를 받아 셌다 — 상태를 잃는 연산 25 · 규칙만 잃는 연산 14. 그 전수가 NC-23 을
냈다.

### NC-23 — 같은 모양의 **다섯 번째**이고, 앞의 넷과 다른 이유로 남았다

앞의 넷(NC-8 · 9 · 13 · 17)은 **지우는 자리**를 좁게 셌다. 이번 것은 자리는 맞게
셌는데 **그 자리에서 사라지는 사실**을 좁게 셌다 — 가드가 그 줄의 σ 한 칸에만 물었다.
직접 재현했다:

```
UPDATE ... SET upper_spec_limit=0.31, center_line=0.11
 WHERE 수입 / 수분 / 액상수지;      ← 고객 도면에서 온 값. σ 는 건드리지 않았다
alembic downgrade                  → 통과. 아무 말도 하지 않았다
남은 줄:  수분 USL=0.5 CL=0.2      ← 0.31 · 0.11 은 사라졌다
```

이 표는 `(공정 × 검사항목 × 자재군)` 유일키라 **무리마다 다른 규격을 허용하도록
설계됐고**, 규격은 「고객이 정한다」(설계 원칙 5). 감사 ④ 가 「세어야 하는 것은
구문이 아니라 사라지는 **사실**」이라고 적었는데 그 문장이 한 칸에만 적용됐다.

**고친 방식**: 값 칸 목록을 한 벌로 모으고(`_VALUE_COLUMNS`) 베끼는 `INSERT` 와
가드가 **같은 목록을 부르게** 했다. 가드는 지울 줄과 남길 줄의 값을 줄 대 줄로
견준다. σ 와 σ출처는 앞 가드가 이미 보고, σ 가 비면 양방향 CHECK 가 σ출처를 하나로
못박으므로 뺐다 — 이유를 코드에 적었다.

### NC-26 — 항목을 넣은 세션이 그 항목으로 쟀다

W-1 을 `audit-data` 에 넣은 것도, 그것으로 잰 것도 같은 회차(④)였다. **「무엇을
묻는지」가 옳은지는 아무도 보지 않은 상태였고**, ⑤ 가 그것을 봤다. 셋이 나왔다 —

1. 구문 열이 **원시 SQL** 이라 Alembic 저장소에서 「기존 행 전체를 검증하는가」가
   `grep` **0 건**이다(실제 값: `ADD CONSTRAINT ... CHECK` 0 · `op.create_check_constraint` 4).
   감사 ④ 가 그 자리에 닿은 것은 항목을 따라간 덕이 아니라 파일을 통째로 읽은 덕이다
2. **W-4(잠금이 언제 풀리는가)의 기한이 지났다** — 「감사 ⑤ 전 · 이 조각의 마감」이었다.
   조각에 붙였는데도 넘어갔다
3. 「못 재니까 적합」은 막았는데 **무엇이 갖춰지면 적합인가**가 비어 있었다. 글자대로
   읽으면 이 항목은 영원히 부적합이고, 그러면 NC-21 의 고침이 무엇을 닫았는지 알 수 없다

셋 다 고쳤다 — 파이썬 API 대응을 병기하고, 여섯째 눈을 넣고, 판정 규칙을 표로 적었다
(적혀 있지 않으면 부적합 · 적혀 있으면 적합 + 심각도 확인불가).

### 새로 세운 검사 둘 — 여섯 갈래를 밟는다

| 돌연변이 | 무엇을 어긋냈나 | 물었는가 |
|---|---|---|
| **M-1** | 값 비교 가드를 `AND FALSE` 로 | 물었다 — `..._a_group_specific_spec_would_vanish` |
| **M-2** | `code_groups` 가드를 `IF FALSE AND` 로 | 물었다 — `..._the_group_someone_changed_would_vanish` |
| **M-3** | `_COMPARED_VALUE_COLUMNS` 에서 `unit` 제거 | 물었다 — 같은 테스트의 `unit` 갈래 |

**M-3 이 목록을 한 벌로 모은 값을 잰다.** 칸 하나를 빼면 그 칸의 갈래가 즉시 실패하므로,
「가드의 넓이를 사람이 지킨다」가 아니라 검사가 지킨다.

셋 다 `git diff --numstat` 으로 들어간 것을 확인한 뒤 돌렸다 — ③ 이 공백 하나로,
④ 가 0 번 치환으로 지날 뻔한 자리다.

**거짓 멈춤이 없다는 것도 확인했다.** 정상 설치 경로(빈 DB → 마이그레이션 → 시드 →
downgrade → 다시 upgrade → downgrade)가 한 번도 멈추지 않는다.

### 반박하지 않았다

다섯 다 코드에서 확인했다. NC-23 은 **직접 재현해서**, NC-25 는 `code_groups` 를
건드리는 테스트가 한 줄도 없다는 것을 보고, NC-26 은 **grep 을 실제로 돌려**(구문 열
0 건), NC-24 · NC-27 은 구문을 세어(`create_check_constraint` 4) 확인했다.

**NC-23 의 제안 둘 중 첫째를 골랐다.** 둘째(「규격은 항목이 갖는 것」을 CHECK 로
거는 것)는 이 표가 무리별 규격을 **허용하도록** 설계된 것과 정면으로 부딪친다 —
설계 원칙 5 가 「규격은 고객이 정한다」이고, 무리가 다르면 고객도 다를 수 있다.

관찰 셋은 고칠 것이 없다는 판정에 동의한다. **OB-11(죽은 `UPDATE`)만 남겨 둔다** —
지우면 「지우는 자리」를 세는 다음 사람이 한 줄 덜 보게 되는데, 지금은 그 자리에
가드가 있다는 표시로도 읽힌다. W-6 이 형태를 정할 때 함께 정리한다.

---

## 감사 ⑤-b — 같은 커밋을 본 두 번째 감사 (2026-09-16, 좁힘)

- **감사한 커밋**: `30de559` — ⑤-a 와 **같다**. 두 세션이 서로 모르는 채 돌았다
- **반영**: 이 커밋
- **결과**: 여섯을 닫고(NC-17~22, ⑤-a 와 같다), 넷을 냈으며 그중 **셋이 새것**이다

**둘이 독립으로 같은 자리를 짚었다.** ⑤-a 와 ⑤-b 가 서로 모르는 채 「되돌리기가
지워질 줄의 값을 가져간다」와 「독스트링이 자기 코드와 모순된다」를 둘 다 냈다.
**같은 결함을 두 번 찾는 것은 낭비가 아니다** — 한 감사가 놓쳤을 확률을 제곱으로
줄인다. 그리고 각자 상대가 못 본 것을 하나씩 더 봤다.

### ⑤-b 만 본 것 셋

**NC-28 — NC-16 의 좁힘이 upgrade 설명에 남아 있었다.** 가드는 「임의와 실측 둘 다」로
넓어졌는데 머리말은 아직 「**실측** σ 나 고객이 준 규격이 따라간다」였다. `UPDATE` 로
줄을 옮기므로 **칸을 고르지 않는다** — 「임의」 σ 도 따라간다. 같은 명제가 가드에서는
넓고 설명에서는 좁았다.

**NC-29 — `pg_locks` 를 실제로 폴링해 표 수가 틀린 것을 잡았다.** 「네 표의 쓰기가
멈춘다」로 적혀 있었는데 `code_groups` 는 `RowExclusiveLock` 뿐이라 다른 쓰기를 막지
않는다. 구문으로도 확인된다 — 이 리비전이 그 표에 하는 일은 `SELECT` · `INSERT` ·
`DELETE` 뿐이고 DDL 이 없다. **쓰기가 멈추는 것은 셋**이다: DDL 을 받는 `items` ·
`process_inspection_standards` 와 외래키가 가리키는 `common_codes`.

**NC-30 — 넷째 눈의 셋째 구멍.** ⑤-a 가 「구문 열이 원시 SQL 이라 0 건」과 「적합으로
가는 길이 없다」를 냈고, ⑤-b 는 **「적혀 있기만 하면 적합이 나온다」**를 냈다. 이
저장소가 그 비용을 이미 보였다 — 넷째 눈이 NC-21 을 냈고, 그 고침이 쓴 문단의 수 둘이
틀렸는데(NC-27 · NC-29) **항목을 다시 돌리면 「적혀 있다 → 적합」이 나온다.** 부적합을
낸 눈이 그 부적합의 고침을 검사하지 못한다.

그래서 넷째 눈을 둘로 갈랐다 — 「적혀 있는가」와 **「적힌 것이 앞 셋에서 센 구문과
맞는가(수까지)」**. 앞 셋이 이미 센 목록이 손에 있으므로 **대조는 공짜다.** 판정 규칙에도
줄을 더했다: **틀린 수는 적지 않은 것보다 나쁘다** — 배포하는 사람이 그것을 믿는다.

### ⑤-b 가 새로 본 관찰

**OB-12 — 남길 한 줄을 정하는 것은 `_FIRST_GROUP` 이 아니라 `id` 순서다.** 운영자가
한 줄을 지웠다 다시 등록하기만 해도 원본과 복사본이 뒤집힌다. **NC-23 의 가드는 이미
이것을 견딘다** — 「남을 줄 = 원본」을 전제하지 않고 지울 줄과 남을 줄을 **줄 대 줄로**
견주기 때문이다. 어느 쪽이 원본이든 값이 다르면 멈춘다.

**⑤-b 의 OB-10(그룹 줄이 아직 세 벌)에 동의한다.** 네 방향 전부 검사가 무는 것을
⑤-b 가 확인했고, 붙들고 있는 것이 구조가 아니라 검사라는 구분은 **W-6 의 재료**다 —
「한 벌로 모은 것」과 「검사가 붙드는 것」은 다른 등급의 안전이다.

### 갈린 판단 하나 — W-4

⑤-a 는 **기한 초과**로 보고 「여섯째 눈을 지금 넣어야 한다」로 갔고(NC-26),
⑤-b 는 **등록대로 두는 쪽**이 맞다고 봤다(「이 레포에서 한 번 재 본 것이다」).
**⑤-a 쪽으로 갈랐다** — 값을 더 보고 넣는 판단 자체는 옳지만, W-1 이 「재감사 전」이라는
기한으로 세 회차를 지나간 전례가 있고 이 항목은 **넣어 두어도 잃는 것이 없다**(틀리면
다음 회차가 낸다). 넣은 채로 ⑥ 이 그 눈의 값을 잰다.

---

## 아직 아무도 보지 않은 것

두 회차 모두 목적을 **데이터 · 쓰기 경계 하나**로 좁혔다. 좁혔기 때문에 깊이
보였고, 좁혔기 때문에 아래는 **아직 아무도 보지 않았다.**

| 무엇 | 감사자 | 언제 |
|---|---|---|
| 검사 장치 전반 — 테스트가 옳은 것을 보는가 · CI 게이트 · 커버리지의 빈자리 | `audit-quality` | 2단계의 표가 다 선 뒤 |
| 시크릿 · 권한 경계 | `audit-secrets` | 첫 쓰기 엔드포인트가 서는 조각 |
| 관측 · 되돌림의 운영 면 — 백업 · 롤백 절차 | `audit-ops` | 배포처가 생기는 단계 |
| `docs/` 전반이 코드와 맞는가 — ② 는 §5 · §6 을 표본으로만 봤다 | `audit-internal` | 조각 서너 개 머지한 뒤 |
| 자재군 배정이 실제 자재 성상과 맞는가 (`RM-12` · `RM-14`) | — | **코드로 판정할 수 없다. 사람이 본다** — 다만 NC-17 이 그 판단을 지우고 있었다. 사람이 보기 전에 그 자리를 먼저 닫았다 |

---

## 사본 특화 대기

`.claude/agents/` 의 사본은 이 레포에 맞게 좁히라고 두는 것이다. 다만 그 항목은
**첫 감사를 돌린 뒤 실제 발견에서** 뽑는다 — 미리 지어낸 항목은 재지 않고 단정한
것이기 때문이다 (`.claude/agents/README.md`).

아래는 그 사이에 나온, **타당하지만 아직 잴 대상이 없어서** 미룬 항목이다.
버린 것이 아니라 순서를 둔 것이다.

| # | 어디에 | 무엇을 | 왜 미뤘나 | 기한 |
|---|---|---|---|---|
| W-1 | `audit-data` 항목 3 | 기존 행 검사에 **DDL 잠금 위험**을 더한다 — 잠금 시간 · 온라인 인덱스 생성 · 단계적 백필. 운영 데이터가 쌓인 표에 기본값·제약·인덱스를 더하는 마이그레이션은 논리적으로 통과하면서도 배타적 잠금으로 모든 쓰기를 멈출 수 있다 | 1단계에는 운영 데이터가 없고 마이그레이션도 하나뿐이다. 지금 넣으면 재 본 적 없는 위험에 대해 지어낸 항목이 된다 | ~~PR #2 재감사 전~~ · **닫힘 — 사본에 반영됨** |
| W-2 | `audit-data` 항목 4 | 트랜잭션 검사에 **동시성 제어**를 더한다 — 격리 수준 · 행 잠금 · 원자적 갱신 · 낙관적 버전 검사. 한 트랜잭션 안에 묶였다는 것만으로는 갱신 유실과 초과 출고를 막지 못한다 | 1단계는 표를 세우는 단계라 **쓰기 경로가 없다.** 지금 넣으면 모든 감사에서 「해당 없음」으로만 나온다 | ~~PR #2 재감사 전~~ · **관문 1 조각(검사 기록 표가 서는 자리)** — W-1 이 같은 기한으로 세 회차를 놓쳤다. 조각에 붙여 다시 적는다 |
| W-3 | 자재군 · 수입 기준 | **기준이 한 줄도 없는 원자재가 설 수 있다.** 감사 ② 탐침에서 운영자가 넷째 무리 `금속` 을 만들고 그 무리의 원자재를 넣으니 수입 기준이 **0줄**인 채 아무 제약에도 걸리지 않았다. 「분말에 점도를 재라고 내밀지 않는다」는 고쳤는데 「아무것도 재라고 하지 않는다」가 열려 있다 (감사 ② OB-1) | 단순한 CHECK 로 표현되지 않는다 — **「검사 항목이 0개인 합격」을 어떻게 볼 것인가**를 정해야 닫힌다 | **관문 1 조각** |
| W-4 | `audit-data` 항목 3 | DDL 잠금에 **여섯째 눈**을 더한다 — 「이 DDL 이 잡은 잠금이 **언제 풀리는가**」(리비전 끝인가 마이그레이션 끝인가). 다섯 눈은 구문 하나를 보므로, 전체를 한 트랜잭션으로 감싸면 첫 `ALTER` 의 잠금이 데이터 단계를 지나 커밋까지 유지된다는 것을 묻지 않는다 | 감사 ④ 가 **항목 밖에서** 발견했다. 항목만 따라 읽은 다음 사람은 놓친다. 다만 이 레포에서 한 번 재 본 것이라 값을 더 보고 넣는다 | ~~감사 ⑤ 전 · 이 조각의 마감~~ · **닫힘 — 사본에 반영됨(NC-26).** 기한이 지난 채 ⑤ 를 맞았고, 그것을 ⑤ 가 NC-26 으로 냈다 |
| W-5 | 자재군 그룹 · 되돌림 | **올릴 때 조용한 것이 내릴 때 막는다.** 운영자가 `MATERIAL_GROUP` 그룹의 설명만 고쳐 두면 upgrade 는 넘어가고 downgrade 는 영구히 멈춘다 — 사람이 설명을 되돌리기 전까지. 되돌릴 수 없는 상태가 되었다는 것을 아무도 미리 알지 못한다 (감사 ④ OB-8) | 멈추는 쪽이 설계다(「지울지는 사람이 정한다」). 고칠 것이 아니라 **언제 말해 줄 것인가**의 문제이며, 드러나는 것은 화면이 서는 날이다 | **화면이 서는 단계.** 감사 ⑤ 의 OB-12(가드가 멈추면 빠져나갈 문이 없다 — 사람이 되돌릴 값을 모를 수 있다)를 여기 붙인다 |
| W-6 | 되돌림 전반 | **「지우는 자리」를 사람이 매번 세지 않는 방법을 정한다.** NC-8 · NC-9 · NC-13 · NC-17 이 같은 모양이고, 매번 「전부 다시 봤다」로 닫았는데 매번 하나가 남았다. 셋째 회차는 `DELETE` 를 셋 세었고 넷째 회차가 그 밖의 `drop_column` 을 찾았다. **「이 리비전이 만들지 않은 것은 되돌릴 때 건드리지 않는다」를 한 곳에서 표현하는 형태**가 필요하다 | 같은 종류가 넷이면 원인은 그 코드가 아니라 **설계 단계**에 있다. 다만 지금 지어내면 재 본 적 없는 추상이 된다 — 2단계에서 되돌림이 있는 리비전이 둘 더 서면 형태가 보인다 | **2단계 마감 전.** NC-23 이 근거를 다섯째로 늘렸고, 정할 형태가 「어떤 연산인가」에 더해 **「그 줄의 어떤 칸이 사람의 것인가」**까지 물어야 한다는 것이 ⑤ 의 입력이다. OB-11(죽은 `UPDATE`)도 여기서 함께 정리한다 |

**기한은 전부 조각에 붙인다.** 「재감사 전」으로 적었던 것이 W-1 을 세 회차 놓치게
했다 — 그것은 **아무 회차에도 속하지 않는 기한**이라 지나가도 아무것도 드러나지
않는다. 조각에 붙은 기한은 그 조각을 여는 사람이 보게 되고, 그 사람에게는 차례가
있다.

### W-1 은 닫혔다 — 사본에 반영됨

`.claude/agents/audit-data.md` 에 「항목 3 — 통과하는 것과 멈추지 않는 것은
다르다」를 넣었다. 묻는 것은 **잠금이 얼마나 걸리는가가 아니라 이 DDL 이 무엇을
하는가**다 — 표를 다시 쓰는가 · 기존 행 전체를 검증하는가 · 잠금을 잡고 인덱스를
만드는가 · 그 사실이 리비전에 적혀 있는가 · 피할 수 있는 형태가 있는가. 다섯 다
**구문만 읽고** 판정되므로 세 도구로 잰다. 심각도는 트래픽과 행 수에 달려 지금은
잴 수 없으니 **전제를 먼저 적고 확인불가**로 두게 했다 — 「못 재니까 적합」이 되는
길을 함께 막았다.

**사본 특화이므로 원본(`claude-kit`)으로 되먹이지 않는다.** 이 항목이 값을 내는지는
아직 이 레포에서만 재 봤다. 닫은 것은 「사본에 들어갔다」까지이고, **항목이 실제로
무엇을 잡는지는 감사 ④ 가 잰다.**

**왜 세 회차를 놓쳤나.** 기한을 「재감사 전」으로 적었는데 그것은 **아무 회차에도
속하지 않는 기한**이었다 — 감사는 항목을 들고 돌 뿐이고, 회차와 회차 사이에 항목을
고치는 일은 누구의 차례도 아니다. 그래서 세 번 지나가는 동안 기한이 지났다는 것조차
아무것도 드러내지 않았다. W-2 · W-3 의 기한(「관문 1 조각」 · 「다음 조각」)은 **조각에
붙어 있어** 그 조각을 여는 사람이 보게 된다. 다른 모양이다.

W-2 는 둘 중 더 뾰족하다. 항목 4 가 「하나의 트랜잭션 안에 있는가」만 물어서
**적합으로 판정하고 지나갈** 경로이기 때문이다. 2단계(IQC 판정 → 로트 생성)에서
재고 차감이 서는 순간 잴 대상이 생긴다.

> **감사 ①이 이미 돌았다.** W-1 은 그 감사에서 실제로 걸렸어야 할 자리를 하나
> 지나쳤다 — 이 리비전은 기존 행이 있는 표에 CHECK 와 유일키를 더하고 `SERIAL`
> 기본키를 붙이므로 표를 다시 쓴다. 오늘은 운영 데이터가 없어 무해하지만,
> **항목이 들어간 지금 감사 ④ 가 그 자리를 잰다.** W-2 는 여전히 쓰기 경로가
> 없어 잴 대상이 없다 — 검사 기록 표가 서는 다음 조각에서 생긴다.

## 출처

W-3 은 감사 ②의 관찰 OB-1 이다 —
[기록](https://github.com/pdw96/ERP/pull/2#issuecomment-5694335608).

W-1 · W-2 는 `pdw96/ERP#3` 에서 Codex 리뷰가 낸 P2 다. 같은 리뷰가 낸 나머지 열은 그
PR 에서 고쳤다 — 그쪽은 지시가 감사자에게 **거짓 판정을 내게 하던** 자리였고,
이 둘은 없던 체크 항목을 더하는 쪽이라 갈랐다.

- [#discussion_r4023206001](https://github.com/pdw96/ERP/pull/3#discussion_r4023206001) — W-1
- [#discussion_r4023206013](https://github.com/pdw96/ERP/pull/3#discussion_r4023206013) — W-2
