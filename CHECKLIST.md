# 머지 전 확인

## 항상

- [ ] **검사 넷 통과** — `ruff check` · `ruff format --check` ·
      `mypy app migrations` · `pytest` (실제 PostgreSQL 에 붙어서)
- [ ] 관련 테스트 통과 — 실패를 「알려진 실패」로 넘기지 않는다
- [ ] 테스트를 건너뛰게(`skip`) 만들어 통과시키지 않았다
- [ ] 새 의존성이 있다면 도입 이유가 세 줄 적혀 있다
- [ ] 적어 두기만 하고 강제하지 않는 규칙을 새로 만들지 않았다

## 앵커볼트를 건드렸다면

- [ ] **스키마 변경에 마이그레이션이 있다** — 모델만 고치고 넘어가지 않았다
- [ ] **마이그레이션이 모델과 같은 표를 만든다** — 자료형 · 널 허용 · 기본키 ·
      외래키 · 유일키 · CHECK 까지 비교하는 테스트가 통과한다
- [ ] **리비전을 낸 뒤 `pytest tests/test_migrations.py` 를 돌렸다** — 자동
      생성이 식을 바꿔 박는 일이 실제로 있었다
- [ ] 되돌리는 방법이 있다 — `alembic downgrade` 가 실제로 돈다
- [ ] 공통코드 그룹을 더하거나 지웠다면 `CODE_GROUPS` 와 시드 SQL 이 함께 갔고
      둘이 어긋나지 않는지 테스트가 본다

## 데이터를 건드렸다면

- [ ] **시드가 트랜잭션 하나다** — 일부러 중간에 터뜨리면 품목 표가 0행이고,
      다음 시도가 정상적으로 채운다
- [ ] 불리언 칸에 `1`/`0` 이 아니라 `TRUE`/`FALSE` 가 들어 있다
- [ ] 비워 두기로 한 값(완제품 안전재고 · σ · 조정사유)을 지어내지 않았다
- [ ] 「오늘」이 나오는 데이터를 SQL 에 넣지 않았다

## 1단계를 닫기 전

- [ ] PostgreSQL 16 에서 **실제로 돌렸다** — 마이그레이션 → 시드 → 재기동 시
      시드 건너뛰기
- [ ] 2단 BOM 이 실제로 두 단이다 — 완제품 ← 반제품 ← 원자재
- [ ] 확장 표 셋이 (그룹, 코드) 복합 외래키로 묶여 있다
- [ ] 로트가 한 표다
- [ ] **프론트엔드 디렉터리가 없고 공개 API 가 없다**
- [ ] `docs/schema.md` 의 미결 넷이 그대로 미결인지, 닫혔다면 문서가 고쳐졌는지
      확인했다 — 단위 변경 · BOM 손실률 · 검사 기준 칸 넷 · FIFO 정렬 키

## 배포 전

> 1단계에는 배포처가 없다. 이 절은 화면이 서는 단계에서 채운다.

- [ ] 프로덕션 에러의 원인을 10분 안에 찾을 경로가 있다
- [ ] 롤백 절차를 실제로 한 번 해 봤다
