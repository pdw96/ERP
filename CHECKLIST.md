# 머지 전 확인

## 항상

- [ ] **CI 의 `backend` 잡이 초록** — 그 잡이 무엇을 도는지는 `ci.yml` 한 자리가
      든다. **아래는 그중 로컬에서 바로 돌릴 수 있는 것들이고 전부가 아니다**
      (감사 ⑱ NC-167): `ruff check` · `ruff format --check` ·
      `mypy app migrations` · `pytest` (실제 PostgreSQL 에 붙어서)
- [ ] 관련 테스트 통과 — 실패를 「알려진 실패」로 넘기지 않는다
- [ ] 테스트를 건너뛰게(`skip`) 만들어 통과시키지 않았다
- [ ] 새 의존성이 있다면 도입 이유가 세 줄 적혀 있다
- [ ] 적어 두기만 하고 강제하지 않는 규칙을 새로 만들지 않았다
- [ ] **고친 자리의 주석과 독스트링을 함께 봤다** — `CLAUDE.md` 가 그것을 기준
      문서로 들고 「맞는 쪽은 코드이고 고칠 것은 주석이다」로 정해 두었는데,
      **고칠 때 함께 보는 절차**가 없어 NC-166 이 그 틈에서 났다(로그 설비를
      세우는 동안 「설비는 아직 없다」는 독스트링이 그대로 남았다)

## 앵커볼트를 건드렸다면

- [ ] **스키마 변경에 마이그레이션이 있다** — 모델만 고치고 넘어가지 않았다
- [ ] **마이그레이션이 모델과 같은 표를 만든다** — 자료형 · 널 허용 · 기본키 ·
      외래키 · 유일키 · CHECK 까지 비교하는 테스트가 통과한다
- [ ] **리비전을 낸 뒤 `pytest tests/test_migrations.py` 를 돌렸다** — 자동
      생성이 식을 바꿔 박는 일이 실제로 있었다
- [ ] 되돌리는 방법이 있다 — `alembic downgrade` 가 실제로 돈다
- [ ] 공통코드 그룹을 더하거나 지웠다면 `CODE_GROUPS` 와 시드 SQL 이 함께 갔고
      둘이 어긋나지 않는지 테스트가 본다

## 데이터를 건드렸다면

- [ ] **시드가 트랜잭션 하나다** — 일부러 중간에 터뜨리면 품목 표가 0행이고,
      다음 시도가 정상적으로 채운다
- [ ] 불리언 칸에 `1`/`0` 이 아니라 `TRUE`/`FALSE` 가 들어 있다
- [ ] 비워 두기로 한 값(완제품 안전재고 · σ · 조정사유)을 지어내지 않았다
- [ ] 「오늘」이 나오는 데이터를 SQL 에 넣지 않았다

## 2단계를 닫기 전

> 1단계는 닫혔다 — 그 항목은 `docs/PRD-1단계.md` 의 성공 기준에 남아 있다.

- [ ] **불합격이 로트를 만들지 못한다** — 주석이 아니라 제약으로 막힌다
- [ ] **특채만이 예외이고, 그 사유가 `special_acceptance_allowed` 인 것만이다**
- [ ] **특채 로트가 `inspection_id` 로 검사를 가리킨다** — 로트에 특채 칸을
      따로 두지 않았다 (같은 사실이 두 곳에 살면 반드시 갈린다)
- [ ] **로트 생성과 수불 원장 줄이 한 트랜잭션이다** — 로트는 생겼는데 원장에
      줄이 없는 상태가 없다
- [ ] **판정 시점의 규격이 박혀 있다** — 규격이 나중에 바뀌어도 옛 판정이
      재현된다. 합격 여부를 저장한 것이 아니라 근거를 저장한 것이다
- [ ] **판정자가 남아 있고 `is_present()` 가 걸려 있다** — `btrim` 은 탭과
      전각 공백을 통과시킨다
- [ ] **자재군 축이 마이그레이션으로 왔다** — 이미 심긴 표라 재시드가 닿지
      않는다. 시드 파일만 고치고 넘어가지 않았다
- [ ] **수량 칸마다 `is_finite()` 가 하한과 함께 걸려 있다** — `NaN >= 0` 은
      참이고, 한 줄이면 이후의 모든 합계가 `NaN` 이 된다
- [ ] **프론트엔드 디렉터리가 여전히 없다** — 쓰기 엔드포인트까지만 섰다
- [ ] **공통코드 그룹이 스물셋에서 스물넷이 됐다** — `MATERIAL_GROUP` 이
      `CODE_GROUPS` 와 시드 SQL 에 함께 갔고, 둘이 어긋나지 않는지 테스트가 본다
- [ ] `docs/schema-2단계.md` 에서 답을 받은 **넷**이 **지어진 것과 같은지** 봤다 —
      규격이 빈 항목의 판정 · 원장 수량의 부호 · FIFO 정렬 키 ·
      로트의 공급사 정체성 (자재군의 형태는 착공에서 닫혔다). 답은 설계에서
      받았고, 닫는 것은 **그 답대로 섰는가**다

## 배포 전

> 1·2단계에는 배포처가 없다(월 비용 한도 0원 · 로컬 compose 뿐). 이 절은
> 화면이 서는 단계에서 채운다. 배포처가 생기는 날 **기준정보 변경을
> 마이그레이션으로 내는 경로**도 함께 열린다 — `docs/schema.md` 미결.

- [ ] 프로덕션 에러의 원인을 10분 안에 찾을 경로가 있다
- [ ] 롤백 절차를 실제로 한 번 해 봤다
