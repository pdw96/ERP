-- 근무형태 셋.
--
-- **365행이 아니라 세 행이다.** 24시간 가동이 캘린더를 두 조 + 사무직으로
-- 줄였다 — 기준정보는 공정 조건을 알면 작아진다.
--
-- 실사 창 09:00–17:30 은 여기 없다. 현장 주간조와 사무직이 **겹치는 구간**이라
-- 계산으로 나오며, 저장하면 근무 시간을 고쳤을 때 둘이 갈린다.
--
-- 야간조는 끝 시각이 시작보다 이르다. 자정을 넘는 것이 정상이다.
INSERT INTO shift_patterns (group_code, code, starts_at, ends_at, on_site) VALUES
  ('SHIFT', '현장주간', '09:00', '21:00', TRUE),
  ('SHIFT', '현장야간', '21:00', '09:00', TRUE),
  ('SHIFT', '사무',     '08:30', '17:30', FALSE);

-- 비가동 구간은 **시드하지 않는다.** 구간이 날짜를 가지므로 기준정보 SQL 에
-- 넣을 수 없다 — 「오늘」이 나오면 파이썬이라는 경계 때문이다. 명절과 설비
-- 보전은 사람이 그때 적는다.
