-r requirements.txt

# **httpx** — FastAPI 의 `TestClient` 가 이것 위에 서 있다. 엔드포인트를 실제로
# 불러 보는 테스트가 서버를 띄우지 않고 앱에 직접 붙을 수 있게 한다. 러너가
# 아니라 테스트가 부르는 것이라 개발 의존성이다.
httpx==0.28.1
pytest==8.3.4
ruff==0.8.4
mypy==1.14.0
