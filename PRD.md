# 조기경보 ERP — 1단계 · 표를 세우는 단 한 번

> 설계도: [조기경보 ERP 설계도 42판](https://claude.ai/artifact/GMJpbFywKh8wj6N4vEukT6)
> 이 문서는 설계도를 대체하지 않는다. **이번 1단계에서 무엇을 짓고 무엇을 짓지
> 않는가**만 정한다. 엔티티의 근거와 판정의 이유는 설계도에 있다.

## 문제

필름·시트를 코팅하고 적층하는 공정에서 물건이 다섯 구간을 지나간다 — 입고 →
원재료창고 → 생산창고 → 제품창고 → 출하. 그 흐름을 적는 장부가 없어서
**지금 재고가 얼마인지, 무엇이 언제 위험해지는지를 장부에서 답할 수 없다.**

설계도가 마흔두 판에 걸쳐 그 장부의 모양을 정했고 여덟 단계로 나눴다. 1단계는
그중 **표를 세우는 단 한 번**이다. 기준정보가 없으면 뒤의 어떤 화면도 기준을
끌어올 수 없고, 나중에 세우면 이미 들어간 사람의 판정을 지우면서 세우게 된다.

## 핵심 사용자와 시나리오

- **사용자**: 우리 회사 한 곳, 소수 인원. 단일 테넌트.
- **시나리오 (1단계)**: 빈 데이터베이스에서 `docker compose up` 하면
  마이그레이션이 끝까지 돌고, 기준정보가 트랜잭션 하나로 들어가고, 품목 코드를
  고르면 그 품목의 공정과 검사 기준 여섯 값이 표에서 따라 나온다.
  **화면은 없다.** 확인은 테스트와 SQL 질의로 한다.

1단계가 끝나면 2단계(IQC 판정 → 자재 재고 로트 생성)가 기준을 새로 정의하지
않고 표에서 읽기만 하면 된다.

## 성공 기준

1. **마이그레이션이 PostgreSQL 16에서 끝까지 돈다.** 그리고 마이그레이션이
   만든 표가 모델과 같다 — 자료형 · 널 허용 · 기본키 · 외래키 · 유일키 ·
   CHECK 까지 비교하는 테스트가 있다.
2. **한 엔진의 방언이 마이그레이션에 박히지 않는다.** CHECK 식을 모델에서
   불러 쓰고, 그것을 테스트가 지킨다 (`trim` / `btrim` 같은 자리).
3. **시드가 트랜잭션 하나다.** 일부러 중간에 터뜨리면 품목 표가 0행이고, 다음
   기동이 정상적으로 채운다. 「반쯤 채워짐」이라는 상태가 없다.
4. **시드 판단이 세 조건이다** — 마이그레이션 완료 · 환경변수 스위치 ·
   품목 표가 비어 있음. 파일 존재로 판단하지 않는다.
5. **품목 20종 + 반제품이 들어가고 2단 BOM이 실제로 두 단으로 전개된다.**
   완제품 ← 반제품 ← 원자재. 반제품이 앉을 자리가 비어 있지 않다.
6. **공통코드 23그룹이 서고, 확장 표 셋이 (그룹, 코드) 복합 외래키로 묶인다.**
   「이 칸에는 이 그룹만」이 문서가 아니라 제약으로 강제된다.
7. **로트가 한 표다.** 자재 로트와 완제품 로트가 갈려 있지 않고, 반제품
   로트가 앉을 자리가 있다.
8. **화면 0 · 공개 API 0.** 프론트엔드 디렉터리가 없다.

## 하지 않을 일

1. **조기경보 층을 만들지 않는다** — 자재 리스크 · 납기 리스크 · 리스크 보드 ·
   LLM 브리핑. 설계도가 「경보 층 — ERP Setup 이후로 보류」로 적어 둔 자리다.
2. **화면을 만들지 않는다** — 프론트엔드 없음, 공개 API 없음. 1단계는 화면이
   없는 단계다. 화면을 먼저 만들면 스키마가 화면을 따라가게 된다.
3. **2~8단계를 하지 않는다** — IQC 판정과 로트 생성 · 수불 원장 · 구매반품 ·
   재고이동 요청/처리 · 생산 흐름과 납기 역산 · IPQC/FQC · 출하 · 경보.
4. **기존 레포(`pdw96/ai-production-risk-early-warning`)에서 코드도 문서도
   가져오지 않는다.** 설계도만 들고 백지에서 짓는다.
5. **나중으로 미룬 기준정보 다섯을 만들지 않는다** — 공정(라우팅 의미의) ·
   라우팅 · 설비/작업장 · 설비 보전 계획 · 단위 환산표. 빈 기준정보는 없는
   기준정보보다 나쁘다.
6. **사용자 계정과 권한을 만들지 않는다.** 메뉴 트리가 그대로 초안이며,
   사람이 늘어나는 날의 일이다.
7. **단가표를 만들지 않는다.** 등급 하향도 정상 단가도 건별 협상이라
   기준정보의 자격이 없다.

## 제약

- **스택**: Python · FastAPI · SQLAlchemy · Alembic · PostgreSQL 16
- **호스팅**: 로컬 Docker Compose만. 배포처 없음.
- **월 비용 한도**: 0원
- **외부 API와 쿼터**: 없음. 1단계는 순수 DB 작업이다.
- **테넌시**: 단일 테넌트. `tenant_id` 를 두지 않는다.
